---
title: Rozenite plugins
summary: Rozenite, the React Native DevTools plugin framework, and its plugins; the escape hatch when a built-in DevTools panel is not enough
status: stub
sources:
  - https://www.rozenite.dev/ (Rozenite and its official plugins)
---

# Rozenite plugins

Reference for **expo-developer-tools**.

TODO. What Rozenite is (a plugin framework whose panels auto-appear inside React Native DevTools and
auto-disable in production), and its official plugins (network-activity with WebSocket coverage,
redux-devtools, tanstack-query, async-storage, mmkv, performance-monitor, expo-atlas). Frame it as
the "when the built-in panel is not enough" reach. Primary sources only; prior-art skills are never a source.
